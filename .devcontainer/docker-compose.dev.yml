services:
  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: syncbot
      MYSQL_ROOT_HOST: "%"
    ports:
      - "3306:3306"
    volumes:
      - syncbot-db:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build:
      context: ..
      dockerfile: Dockerfile
    command: sleep infinity
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - ../.env
    environment:
      # Overrides that are always fixed for local dev
      LOCAL_DEVELOPMENT: "true"
      DATABASE_BACKEND: ${DATABASE_BACKEND:-mysql}
      DATABASE_URL: ${DATABASE_URL:-}
      DATABASE_HOST: ${DATABASE_HOST:-db}
      ADMIN_DATABASE_USER: ${ADMIN_DATABASE_USER:-root}
      ADMIN_DATABASE_PASSWORD: ${ADMIN_DATABASE_PASSWORD:-rootpass}
      ADMIN_DATABASE_SCHEMA: ${ADMIN_DATABASE_SCHEMA:-syncbot}
      DATABASE_TLS_ENABLED: ${DATABASE_TLS_ENABLED:-false}
      DATABASE_SSL_CA_PATH: ${DATABASE_SSL_CA_PATH:-/etc/pki/tls/certs/ca-bundle.crt}
    volumes:
      - ..:/app:cached
    ports:
      - "3000:3000"

volumes:
  syncbot-db:
